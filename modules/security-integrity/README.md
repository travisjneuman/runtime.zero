# Security and integrity module

Development-only evidence classifier for caller-supplied exact digest
observations. Matches/mismatches remain report-only and unknown ownership is
blocked. It does not scan the host, claim malware detection, remediate, quarantine,
or execute anything. Core does not install or execute this package.
