# Uninstall module

Development-only domain classifier for manager-native uninstall evidence. It
maps caller-supplied synthetic records into the shared finding contract. It does
not discover software, run managers/uninstallers, plan writes, or execute an
uninstall. Installed records without manager ownership are blocked. Core does
not install or execute this package.
