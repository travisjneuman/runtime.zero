# Leftovers module

Development-only post-uninstall domain classifier over caller-supplied synthetic
evidence. Only exact runtime-owned orphan/executable evidence may become a
quarantine candidate; protected, user, system, and unknown data is blocked.
This package does not scan filesystems, plan writes, quarantine, or delete. Core
does not install or execute it.
