[package]
name = "rz0-privacy-contract"
version = "0.1.0"
edition = "2024"
authors = ["Travis J. Neuman <travis@neuman.dev>"]
description = "Bounded privacy classification and report-local redaction for runtime.zero"
license = "Apache-2.0"
publish = false
rust-version = "1.96"

[dependencies]
rz0-error-contract = { version = "0.1.0", path = "../error-contract" }
rz0-resource-contract = { version = "0.1.0", path = "../resource-contract" }
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
