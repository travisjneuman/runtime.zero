[package]
name = "rz0-cancellation-contract"
version = "0.1.0"
edition = "2024"
authors = ["Travis J. Neuman <travis@neuman.dev>"]
description = "Allocation-minimal cancellation and deadline primitive for runtime.zero"
license = "Apache-2.0"
publish = false
rust-version = "1.96"

[dependencies]
rz0-error-contract = { version = "0.1.0", path = "../error-contract" }
